language: node_js
node_js:
  - '12'
sudo: required
addons:
  chrome: stable
branches:
  only:
  - develop
  - /^release-[0-999].[0-999]$/
  - master
script:
  - ng test
  - mvn sonar:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.organization=alu0100824810 -Dsonar.login=$SONARs